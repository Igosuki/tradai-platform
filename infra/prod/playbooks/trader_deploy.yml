---
- hosts: trader
  name: Install the trading system
  remote_user: root
  pre_tasks:
    - name: Trader Binary
      copy:
        src: ../../../cargo-target/x86_64-unknown-linux-gnu/release/trader
        dest: /root/trader.staged
        owner: root
        group: root
        mode: '+x'
    - name: Trader Conf
      copy:
        src: ./trader_conf/prod.yaml
        dest: /root/config/prod.yaml
        owner: root
        group: root
        mode: '+x'
    - name: Trader dry conf
      copy:
        src: ./trader_conf/prod_dry.yaml
        dest: /root/config/prod_dry.yaml
        owner: root
        group: root
        mode: '+x'
  tasks:
    - name: backup trader
      shell: cp trader "trader.bak.$(date +%F)"
      args:
        chdir: /root/
      ignore_errors: yes
    - name: restart trader
      shell: systemctl stop trader && systemctl stop trader_dry && cp trader.staged trader && systemctl start trader && systemctl start trader_dry
      args:
        chdir: /root/
